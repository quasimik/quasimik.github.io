{
  "name": "quasimik.github.io",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "publish": "git push origin `git subtree split --prefix build -b master`:master --force",
    "bp": "npm run build && npm run publish",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}